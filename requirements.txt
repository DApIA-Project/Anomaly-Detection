# ML
pip install tensorflow==2.8.0
pip install keras

# not mendatory, as all model are tensoflow based 
# pip install reformer-pytorch==1.4.4
# pip install torch==1.10

# Data processing
pip install pandas
pip install scikit-learn
pip install numpy-typing


# Visualization (chart + maps)
pip install matplotlib
pip install cartopy # needs sudo apt -y install libgeos-dev


# [optional] to run Unit test
pip install pytest

# [optional] graphviz for model visualization
pip install graphviz
sudo apt install graphviz




